# Feature Requests

## 1. Інкрементальне оновлення (Incremental Indexing) ⚡
**Проблема:** Зараз `scan` проходить по всіх файлах. Якщо у вас 10,000 файлів, а ви змінили один, система пересканує все. А головне — `summarize` знову буде мучити LLM для вже відомих функцій.
**Рішення:** Зберігати хеш файлу (або час модифікації) в базі даних.
* При скануванні перевіряти: якщо файл не змінився — пропускати.
* Це зекономить **години** роботи GPU/CPU на великих проектах.

## 2. Гібридний пошук (Hybrid Search) 🔍
**Проблема:** Векторний пошук класний для "змісту" ("як ми працюємо з базою?"), але поганий для точних назв. Якщо ви шукаєте `getUserById`, вектори можуть видати `findClient`, що схоже за змістом, але ви хотіли саме ту функцію.
**Рішення:** Об'єднати векторний пошук (Dense) з пошуком за ключовими словами (BM25 / Sparse).
* Вектори знаходять *концепції*.
* BM25 знаходить *точні збіги*.
* Результати об'єднуються (Reciprocal Rank Fusion).

## 3. Підтримка JavaScript/TypeScript 🌐
**Проблема:** Зараз тільки Python, Go, Kotlin. Веб-розробка займає величезну частку ринку.
**Рішення:** Додати `tree-sitter-javascript` та `tree-sitter-typescript`. Це відкриє інструмент для фронтенд розробників та Node.js бекендів.

## 4. API Сервер (FastAPI) 🔌
**Проблема:** CLI — це добре, але важко інтегрувати з редактором коду.
**Рішення:** Додати команду `code-indexer serve`.
* Запускає локальний HTTP сервер.
* Ви можете написати простий плагін для VS Code або Neovim, який робить запит до `localhost:8000/search?q=...` і показує результати прямо в редакторі.

## 5. Reranking (Пересортування) 🧠
**Проблема:** Bi-Encoder (який ми використовуємо для швидкості) іноді помиляється в нюансах.
**Рішення:** Взяти ТОП-20 результатів пошуку і прогнати їх через Cross-Encoder (більш точна, але повільна модель). Вона пересортує їх і видасть набагато точніший ТОП-5.
